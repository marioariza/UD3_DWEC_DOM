"use strict";

function borrar_horse () {
    let horse = document.getElementById('horse'); // Recogemos el id del div.
    horse.remove(); // Borramos el div recogido anteriormente.
}

function borrar_donkey () {
    let donkey = document.getElementById('donkey');
    donkey.remove();
}

function borrar_cat () {
    let cat = document.getElementById('cat');
    cat.remove();
}