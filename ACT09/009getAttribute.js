"use strict";

let elem = document.querySelector('[data-widget-name]');

alert(elem.getAttribute('data-widget-name')); 

// menu,cogemos el valor del atributo con getAttribute y especificando el nombre del atributo.